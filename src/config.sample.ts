const config = {
  port: 3000,
  jwtSecret: 'insert-secret',
  db: {
    host: 'localhost',
    user: 'user',
    password: 'password',
    database: 'finance-tracker',
    port: 3304,
  }
};

export default config;